# gyoumu-app Development Guide

## Project

このリポジトリは、対話式進学塾1対1ネッツで利用する「個別ミッショントレーニング（gyoumu-app）」の開発プロジェクトです。

- `student-app`（スキマ君）とは別リポジトリです。
- `student-app`は、利用者から明示的に対象指定された場合だけ変更してください。
- netz-osやAI社員プロジェクトとは関係ありません。

## Goal

保守性・拡張性・安定性を維持しながら、機能追加と改善を行います。既存機能と既存データを壊さないことを最優先してください。

## Basic Principles

常に以下を優先してください。

1. 既存機能と既存データを壊さない
2. 依頼された範囲内で最小変更にする
3. 既存APIの互換性を維持する
4. 既存の共通部品を優先して利用する
5. ReactだけでなくGASでも入力・権限を検証する
6. 推測を事実として扱わない
7. 問題はコード経路と事実を確認してから修正する
8. 関係のないリファクタリングを行わない
9. 既存の未コミット変更を勝手に破棄・上書きしない

仕様や設計が曖昧な場合は、調査、設計確認、実装の順で進めてください。業務判断が必要な事項を勝手に決定しないでください。

## Architecture

基本構造は次のとおりです。

```text
React
  ↓
Google Apps Script
  ↓
Google Spreadsheet
```

認証・認可・入力検証など、サーバー側で保証すべき仕様をReactだけで完結させないでください。クライアントから送信されたroleや権限値を信用しないでください。

## Shared Components

以下を共通部品・定義の正本として利用してください。

- 校舎選択: `SchoolSelect`
- 学年選択: `GradeSelect`
- 組織情報: `src/constants/organization.js`

画面ごとに新しい校舎配列や学年配列を作らないでください。共通部品で対応できない要件は、既存利用画面への影響を確認してから拡張してください。

## Account Structure

現時点のアカウント情報の正本は次の4シートです。構造変更を依頼された場合は、この記載も更新してください。

### アカウントマスター

- userId
- password
- isInitial
- passwordUpdatedAt
- role
- enabled
- sukimakunToken
- sukimakunTokenExpire
- createdAt
- updatedAt
- deletedAt

### 生徒マスター

- userId
- school
- name
- nameKana
- grade
- createdAt
- updatedAt

### 講師マスター

- userId
- name
- nameKana
- createdAt
- updatedAt

### 講師担当校舎

- userId
- school
- isPrimary
- enabled
- createdAt
- updatedAt
- updatedBy

## Roles

許可するroleは次の4種類です。

- admin
- head-teacher
- teacher
- student

利用者の明示的な仕様変更がない限り、これ以外のroleを追加しないでください。roleの判定はアカウントマスターを正本とします。

## userId

userIdはgyoumu-appで自動採番しません。ネッツメニューなどで発行済みのIDを登録して使用します。

- 通常の新規IDは半角数字6桁
- 先頭ゼロを保持した文字列として保存
- 例: `037071`
- 既存の管理者ID `admin`は移行済み例外
- 新規登録で英字IDを追加しない
- 入力値を勝手に6桁へゼロ補完しない
- 比較時は既存のuserId正規化仕様を利用する

userId列をスプレッドシートへ書き込む場合は、先頭ゼロが失われない文字列書式を保証してください。

## Initial Password

現時点の業務仕様は次のとおりです。

- student: `netzs` + userId
- teacher、head-teacher、admin: `1234`
- 新規登録時は、teacher、head-teacher、adminのみ`isInitial=true`
- studentは`isInitial=false`で登録し、既存データが`true`でも初回パスワード変更を要求しない

初回ログイン時のパスワード変更対象はteacher、head-teacher、adminのみです。studentには初期パスワードがありますが、初回パスワード変更の対象外です。

React側で初期パスワードを計算せず、GASを正本としてください。これは平文パスワードを使用する暫定的な既存仕様です。ハッシュ化などのセキュリティ変更は、別途明示されたIssueで扱ってください。

パスワードを一覧API、ログ、エラーメッセージへ含めないでください。登録成功直後の明示的なレスポンスと画面表示だけは、現在の業務仕様に従います。

## Grade

正式な保存・表示値は次のとおりです。

- 小1から小6は `小１` から `小６`
- 中1から中3は `中１` から `中３`
- 高1から高3は `高１` から `高３`
- `大学受験`

数字は全角で保存してください。比較時に半角表記を許容する場合も、保存値は正式表記へ戻してください。

## Name Kana

`nameKana`の正式な保存形式は、全角カタカナで姓名間を全角スペース1個とします。

- 例: `ヤマダ　タロウ`
- NFKC正規化とひらがなからカタカナへの変換後、空白を全角スペース1個へ統一する
- 前後空白と連続・混在空白を残さない
- 長音`ー`と中黒`・`を許可する
- スプレッドシートへ直接一括登録するデータも、同じ形式で事前に作成する
- 既存データを仕様変更だけを理由に自動一括変換しない

## Delete

アカウントは物理削除せず、論理削除を基本とします。

- 通常無効化: `enabled=false`、`deletedAt`は空
- 論理削除: `enabled=false`、`deletedAt`にサーバー時刻

通常無効化と削除済みを区別してください。削除済みアカウントを通常更新APIだけで再有効化しないでください。

## Transactions

複数シートを更新する処理では、次を基本としてください。

1. `DocumentLock`を取得する
2. 最新状態を読み直す
3. 書き込み対象をメモリ上で生成する
4. 入力・重複・参照整合性をすべて検証する
5. 全件成功可能と判断してから書き込みを開始する
6. `setValues()`による一括書き込みを優先する
7. 途中失敗時は成功を返さず、取得済みスナップショットから可能な限り復元する
8. 必ずロックを解放する

Google Sheetsの複数シート更新には完全なトランザクション保証がありません。書き込み順序、復元方法、復元にも失敗した場合の残存リスクを報告してください。「一括書き込み」を完全な原子性があるものとして説明しないでください。

## GAS Manual Functions

診断、比較、プレビュー、確認、検証、件数確認、データ整合性確認などの手動実行専用関数を追加する場合は、結果表示用ラッパー関数もセットで追加してください。

例:

```javascript
function runCompareSomethingSummary() {
  const result = compareSomething();
  console.log(JSON.stringify(result, null, 2));
}
```

- 手動関数をCodexが勝手に実行しないでください。
- setup、migration、rollbackなど書き込みを伴う関数は、利用者から明示的な実行指示がある場合だけ実行してください。
- 本番API、`doPost`、login、validateTokenなどへ結果表示用ログを追加しないでください。
- 手動関数の結果にも秘密情報や個人情報一覧を含めないでください。

## Security

次の値をLogger、console、通常レスポンス、エラーメッセージへ出力しないでください。

- password
- token
- sessionToken
- APIキー
- スプレッドシートID
- 氏名やフリガナなどの個人情報一覧
- メールアドレスや電話番号

本番APIへ一時的な調査ログを常設しないでください。ただし、利用者が障害調査のため明示的に求めた構造化ログは、秘密情報・個人情報・認証情報を含まない範囲で追加できます。調査完了後に削除すべき一時ログか、継続利用する運用ログかを区別してください。

管理用APIはAPIキーだけで許可せず、GAS側で管理セッションと現在のadmin roleを検証してください。

## API

既存action名、リクエスト項目、成功レスポンス項目を、利用者の明示指示なしに破壊的変更しないでください。

変更が必要な場合は、次を先に確認・報告してください。

- 呼び出しているReact画面や外部アプリ
- 既存レスポンスとの互換性
- 移行期間中の新旧データへの影響
- エラー時の挙動

非破壊的な項目追加または新action追加で対応できる場合は、それを優先してください。ただし、不要なAPI増加や認証方式の分岐を作らないでください。

## Git and Deployment Rules

利用者が明示的に指示するまで、次を実行しないでください。

- Git commit
- Git push
- clasp push
- GASデプロイ
- Vercelデプロイ
- 本番データ変更

作業開始時に原則として次を確認してください。

```text
git status
git branch --show-current
git log -3 --oneline
```

作業ツリーに未コミット変更がある場合は、依頼内容と関連する差分か確認してください。

- 利用者が「クリーンでなければ停止」と指定した場合は停止する
- 既存差分を土台にする指示がある場合は、その差分を維持して進める
- 無関係な差分がある場合は、上書き・削除・ステージ・コミットしない
- `git reset --hard`や無断の`git checkout --`を使用しない

コミットを依頼された場合も、対象ファイルと無関係な既存差分を同じコミットへ含めないでください。

## CHANGELOG Rules

Issue単位の開発を完了する際は、利用者から明示的な指示がなくても必ず`CHANGELOG.md`を更新してください。gyoumu-appの変更だけでなく、同じIssueに含まれるGAS、スプレッドシート／マスター、API、権限仕様、student-app連携もプロジェクト全体の主記録として扱います。ただし、student-appだけで完結する変更を無理に詳細記載しないでください。

- 同じIssueを複数回重複追加しない
- 既存Issue項目がある場合は新規追加せず、確認結果、Commit、mainマージ、GASデプロイ、Vercelデプロイ、本番確認、student-app影響などを既存項目へ追記・更新する
- 新しいIssueはIssue Historyの最上部へ追加する
- 確認できた事実だけを記載し、未確認事項を確認済みとして扱わない
- 未実施・未確認・未記録の状態は、その状態を明記する

新しいIssueが完了した場合は、Version Historyの現在開発中のVersionへそのIssueを追加してください。Codexがmajor／minor versionを独自に変更してはいけません。

- 新Versionへの切り替えは、利用者がVersion番号を指定した場合、または明確なリリース区切りを指示した場合だけ行う
- Version番号が指定されていない場合は、現在の最新Versionへ追加する
- Issue HistoryとVersion Historyの内容を整合させる

## Testing

変更範囲に応じて、外部通信を伴わない検証を優先してください。

React変更:

```text
npm run build
npm run lint
git diff --check
```

GAS変更:

```text
node --check gas/コード.js
git diff --check
```

- 既存lintエラーと今回追加したエラーを区別してください。
- API action名、送信項目、既存レスポンス項目の維持をコード経路で確認してください。
- 外部API通信、本番結合テスト、負荷テストは、利用者の明示指示がある場合だけ実行してください。
- テストのために本番データを書き換えないでください。
- 実行できなかったテストを成功したものとして報告しないでください。

## Completion Report

最低限、次を報告してください。

- 作業ブランチ
- 変更ファイル
- 実装または調査内容
- API・既存機能への影響
- テスト結果
- 未確認事項と残るリスク
- `git diff --stat`
- `git status --short`

最後に、次を実施したか明記してください。

- Git commit
- Git push
- clasp push
- GASデプロイ
- Vercelデプロイ
- 本番データ変更

## Coding Style

- 既存コードの命名、レスポンス形式、UIデザインへ合わせる
- 新しい共通部品を作る前に既存共通部品を確認する
- 配列indexやシート列番号を新たにハードコードする場合は、正本となるヘッダーや理由を明確にする
- 秘密値、固定スプレッドシートID、本番URLをコードへ直接追加しない
- 表示名と内部IDを分離する
- マスター管理される値をReactへ個別にハードコードしない
- コメントは実装理由や制約が必要な箇所に限定し、コードの単純な言い換えを避ける
